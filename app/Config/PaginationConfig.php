<?php
namespace Config;
use CodeIgniter\Config\BaseConfig;

class MyConfig extends BaseConfig 
{
    public $pagination = "4";
    //need to add this stuff somewhere in controller then use it, no time to figure that out now: 
    //$config = new MyConfig();
    //$promenna = $config->promenna;
}
?>